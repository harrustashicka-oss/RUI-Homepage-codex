# 个人作品集 · HTML 静态版

## 打开与换电脑展示

1. 把整个文件夹复制到新电脑，或把压缩包先完整解压。
2. 双击 index.html，使用 Chrome、Edge、Firefox 或 Safari 的较新版本打开。
3. 无需安装 Node.js、pnpm、Python，无需登录、联网、数据库或启动后台。

请不要只复制 index.html，也不要直接在压缩包中双击打开。css、js、assets 必须与 index.html 一起保留。文件夹可改名，也可以放在 U 盘中；所有路径均为相对路径。

所有图片、视频和字体均存放在本地。浏览器首次打开通常要求点击“开启声音”后才能播放首屏音乐；点击作品会打开带播放和音量控制的弹窗。自动播放被浏览器限制时，点击视频播放按钮即可。邮箱与 GitHub 外链需要联网，离线展示其他内容不受影响。

## 日后修改

- index.html：导航、正文、作品、联系方式、首屏视频路径及页面标题。
- css/style.css：颜色、字号、行距、页面排版、动画及移动端适配。
- js/main.js：语言切换、声音开关和作品放大，一般不需要改。
- assets/：首屏、头像和作品素材。
- assets/fonts/：离线字体及许可证。

用记事本或代码编辑器打开这些文件，保存后刷新浏览器即可看到修改，无需编译或重新安装任何东西。请保存为 UTF-8 编码。

### 修改文字

例如：

```html
<h2 data-en="Let the work speak.">作品先说话。</h2>
```

标签之间是中文，data-en 中是对应英文。英文不需要变化的文字可以不写 data-en。修改图片说明时，同时更新 alt 和 data-en-alt。

### 替换图片或视频

最简单的方法是把新素材放到对应 assets 子文件夹，保持原文件名和文件格式。也可以在 index.html 中修改 img 或 source 的 src，例如 ./assets/work/new-project.mp4。路径前使用 ./，不要改为磁盘绝对路径或以 /assets 开头。

首屏视频为 assets/avatar-animation.mp4，对应占位图是 assets/hero-video-poster.png。更换首屏视频时，请同时把占位图替换为新视频第一帧。

蓝牙音响作品保留 WebM 压缩版和 MP4 兼容版。换成其他视频时，两个 source 都应改为新作品对应文件，或者只保留一个 MP4 source；请勿混用不同作品。

### 增删作品

index.html 的“作品”区域内，每个 button.project-card 到对应的结束标签是一个完整作品。复制整个块可以增加作品，删除整个块可以移除作品。修改其中的中英文标题、描述、标签、素材路径，并给 data-project 一个不重复的名称。

作品弹窗直接读取卡片内容，无需同步修改其他配置文件。

## 在线发布

这套文件也能直接上传到静态网站托管服务。GitHub Pages 工作流已经改成直接发布 HTML / CSS / JS / 素材，不需要安装项目依赖或构建 React。

GitHub 公开地址：https://harrustashicka-oss.github.io/RUI-Homepage-codex/

把修改同步到仓库 main 分支后，GitHub Pages 自动发布。修改本地文件本身不会自动更新网上页面。旧的 chatgpt.site 地址属于另一个托管平台，不会随 GitHub 自动更新。

## 项目保存

.git 保留改版前的历史，旧项目可从版本记录恢复；便携压缩包不包含该历史或管理文件。.github 是 GitHub Pages 发布设置，.openai/hosting.json 只保留原托管项目标识与静态目录配置，不运行后台或数据库。

改版前的版本标记为 before-static-html。旧依赖、构建缓存和原 public 素材目录已移到项目旁边的“_旧框架备份_静态转换”目录。这份备份不参与新网站运行，也不需要复制到展示电脑。

纯静态版本没有接收表单、用户账号或数据库写入功能；当前作品集的展示交互不依赖这些功能。
